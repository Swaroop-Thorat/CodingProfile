class Solution {
    public int arraySum(int arr[]) {
        // code here
        return sum(arr,arr.length-1);
    }
    int sum(int[] arr,int n){
        if(n==0) return arr[n];
        return sum(arr,n-1)+arr[n];
    }
}