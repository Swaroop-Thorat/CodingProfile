/**
 * Definition for a binary tree node.
 * public class TreeNode {
 *     int val;
 *     TreeNode left;
 *     TreeNode right;
 *     TreeNode() {}
 *     TreeNode(int val) { this.val = val; }
 *     TreeNode(int val, TreeNode left, TreeNode right) {
 *         this.val = val;
 *         this.left = left;
 *         this.right = right;
 *     }
 * }
 */
class Solution {
    public boolean isBalanced(TreeNode root) {
        return dfs(root)!=-1;
    }
    int dfs(TreeNode node){
        if(node==null) return 0;
        int left=dfs(node.left);
        if(left==-1) return -1;
        int right=dfs(node.right);
        if(right==-1) return -1;
        if(Math.abs(left-right)>1){
            return -1;
        }
        return Math.max(left,right)+1;    
    }
}

/* ANOTHER APPROACH:

class Solution {
    public boolean isBalanced(TreeNode root) {
        int[] is={0};
        dfs(root,is);
        return is[0]==0;
    }
    int dfs(TreeNode node,int[] is){
        if(node==null) return 0;
        int left=dfs(node.left,is);
        int right=dfs(node.right,is);
        if(Math.abs(left-right)>1){
            is[0]=-1;
        }
        return Math.max(left,right)+1;    
    }
} */